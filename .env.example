# Frontend API origin. Leave empty when using a reverse proxy in production.
VITE_API_URL=http://localhost:8000

# Optional future OMR integration. Keep provider keys server-side only.
# OMR_PROVIDER_URL=
# OMR_PROVIDER_API_KEY=